
      // auto generated
      //var self;

      // class constructor:
      function QTreeWidgetItem() {
        

        // should be QTreeWidgetItem_BaseJs.call(this, engine):
        //QTreeWidgetItem.prototype = new QTreeWidgetItem_BaseJs(engine);

        var wrapper;

        // creating JS class for existing wrapper:
        if (arguments.length===3 && arguments[0]==="__GOT_WRAPPER__") {

          if (arguments[2]!==undefined 
            && (arguments[2]===null || 
            (typeof(arguments[2].getWrappedType)==="function" && 
            arguments[2].getWrappedType()===
              
                  RJSType_QTreeWidgetItem.getIdStatic()
                
            ))) {

            wrapper = arguments[2];
            if (wrapper!=null) {
              //wrapper.__WRAPPER__ = true;
              Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
            }
            //if (arguments[1]!==true) {
              // only copy properties if this is not an existing wrapper:
              copyProperties(this, wrapper, QTreeWidgetItem);
            //}
          }
          else {
            qWarning("QTreeWidgetItem.js: Constructor called with __GOT_WRAPPER__, but no valid wrapper provided: " + arguments[2]);
            console.trace();
          }

        }
        else {
          
      if (arguments.length >= 2 &&
          arguments.length <= 3) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 2 &&
          arguments.length <= 3) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 2 &&
          arguments.length <= 3) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 2 &&
          arguments.length <= 3) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 1 &&
          arguments.length <= 2) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 1 &&
          arguments.length <= 2) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 1 &&
          arguments.length <= 2) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length >= 0 &&
          arguments.length <= 1) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else 
  
      if (arguments.length == 1) {
    
            self = this;
            wrapper = new QTreeWidgetItem_Wrapper(
              // RJSApi:
              handler
              , arguments[0]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QTreeWidgetItem);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QTreeWidgetItem);
  }

  
  else {
    
        print("QTreeWidgetItem(): wrong number / type of arguments");
      
    console.trace();
  }
  
        }

        //self = this;
        //if (typeof(this.wrapper)!=="undefined") {
        //  this.wrapper.setEngine(engine);
        //}


        if (typeof(wrapper)!=="undefined") {
          //var localSelf = this;
          //print("QAction self:", localSelf);
          // TODO:
          //this.wrapper.triggered.connect(function(checked) { print("action triggered. self:", localSelf); localSelf.triggeredEmitter(checked); });
          

          // signal aliases:
          if (Object.getPrototypeOf(this)!=null) {
            
          }
        }
      }

      //QTreeWidgetItem.prototype = new QTreeWidgetItem_BaseJs(engine);
      //QTreeWidgetItem.prototype = new QTreeWidgetItem_Wrapper(engine);
      QTreeWidgetItem.prototype = new Object();

      QTreeWidgetItem.prototype.toString = function() {
          //return "QTreeWidgetItem [JS] [0x" + this.getAddress().toString(16) + "]";
          return "QTreeWidgetItem [JS]";
        };
      QTreeWidgetItem.getObjectType = function() {
        
            return RJSType_QTreeWidgetItem.getIdStatic();
          
      };

      QTreeWidgetItem.prototype.getObjectType = function() {
        
            return RJSType_QTreeWidgetItem.getIdStatic();
          
      };

      QTreeWidgetItem.prototype.isOfObjectType = function(t) {
        
            if (t===RJSType_QTreeWidgetItem.getIdStatic()) {
              return true;
            }
          

        return false;


        
      };

      // enum values:
      
  // enum: ItemType
QTreeWidgetItem.Type = QTreeWidgetItem_Wrapper.Type;
QTreeWidgetItem.UserType = QTreeWidgetItem_Wrapper.UserType;

  // enum: ChildIndicatorPolicy
QTreeWidgetItem.ShowIndicator = QTreeWidgetItem_Wrapper.ShowIndicator;
QTreeWidgetItem.DontShowIndicator = QTreeWidgetItem_Wrapper.DontShowIndicator;
QTreeWidgetItem.DontShowIndicatorWhenChildless = QTreeWidgetItem_Wrapper.DontShowIndicatorWhenChildless;


      // functions:
      

      // static functions:
      

      // constants:
      

      // public static properties without access function:
      

      // copy function:
      //QTreeWidgetItem.prototype.copy = function() {
      //  return this.wrapper.copy();
      //};

      // destroy function:
      //QTreeWidgetItem.prototype.destr = function() {
      //  return this.wrapper.destr();
      //};
    