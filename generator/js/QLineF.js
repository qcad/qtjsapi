
      // auto generated
      //var self;

      // class constructor:
      function QLineF() {
        

        // should be QLineF_BaseJs.call(this, engine):
        //QLineF.prototype = new QLineF_BaseJs(engine);

        var wrapper;

        // creating JS class for existing wrapper:
        if (arguments.length===3 && arguments[0]==="__GOT_WRAPPER__") {

          if (arguments[2]!==undefined 
            && (arguments[2]===null || 
            (typeof(arguments[2].getWrappedType)==="function" && 
            arguments[2].getWrappedType()===RJSType_QLineF.getIdStatic()))) {

            wrapper = arguments[2];
            if (wrapper!=null) {
              //wrapper.__WRAPPER__ = true;
              Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
            }
            //if (arguments[1]!==true) {
              // only copy properties if this is not an existing wrapper:
              copyProperties(this, wrapper, QLineF);
            //}
          }
          else {
            qWarning("QLineF.js: Constructor called with __GOT_WRAPPER__, but no valid wrapper provided: " + arguments[2]);
            console.trace();
          }

        }
        else {
          
      if (arguments.length == 4) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2], arguments[3]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 4) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2], arguments[3]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 4) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1], arguments[2], arguments[3]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 2) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 2) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 2) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0], arguments[1]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 1) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              , arguments[0]
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 0) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 0) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else 
  
      if (arguments.length == 0) {
    
            self = this;
            wrapper = new QLineF_Wrapper(
              // RJSApi:
              handler
              
              ////this.setWrapper(this.wrapper);
              //Object.setPrototypeOf(this, wrapper);
            );
            //wrapper.__WRAPPER__ = true;
            Object.defineProperty(wrapper, "__WRAPPER__", { configurable: true, writable: true, value: true });
          

        copyProperties(this, wrapper, QLineF);

        //this.setWrapper(this.wrapper);

      

        //copyProperties(this, wrapper, QLineF);
  }

  
  else {
    
        print("QLineF(): wrong number / type of arguments");
      
    console.trace();
  }
  
        }

        //self = this;
        //if (typeof(this.wrapper)!=="undefined") {
        //  this.wrapper.setEngine(engine);
        //}


        if (typeof(wrapper)!=="undefined") {
          //var localSelf = this;
          //print("QAction self:", localSelf);
          // TODO:
          //this.wrapper.triggered.connect(function(checked) { print("action triggered. self:", localSelf); localSelf.triggeredEmitter(checked); });
          

          // signal aliases:
          if (Object.getPrototypeOf(this)!=null) {
            
          }
        }
      }

      //QLineF.prototype = new QLineF_BaseJs(engine);
      //QLineF.prototype = new QLineF_Wrapper(engine);
      QLineF.prototype = new Object();

      QLineF.prototype.toString = function() {
          //return "QLineF [JS] [0x" + this.getAddress().toString(16) + "]";
          return "QLineF [JS]";
        };
      QLineF.getObjectType = function() {
        return RJSType_QLineF.getIdStatic();
      };

      QLineF.prototype.getObjectType = function() {
        return RJSType_QLineF.getIdStatic();
      };

      QLineF.prototype.isOfObjectType = function(t) {
        if (t===RJSType_QLineF.getIdStatic()) {
          return true;
        }

        

        return false;


        
      };

      // enum values:
      

      // functions:
      

      // static functions:
      

        // static function 
        QLineF.fromPolar = function() 
          
        {
          //print("JS: QLineF.fromPolar");
          
      if (arguments.length == 2) {
    
                // calling static wrapper:
                return QLineF_WrapperSingletonInstance.fromPolar(
                  arguments[0], arguments[1]
                );
              

        //copyProperties(this, wrapper, QLineF);
  }

  
  else {
    
        print("QLineF.fromPolar(): wrong number / type of arguments");
      
    console.trace();
  }
  
        };
      

      // constants:
      

      // public static properties without access function:
      

      // copy function:
      //QLineF.prototype.copy = function() {
      //  return this.wrapper.copy();
      //};

      // destroy function:
      //QLineF.prototype.destr = function() {
      //  return this.wrapper.destr();
      //};
    